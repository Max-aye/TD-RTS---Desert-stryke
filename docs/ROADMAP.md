# Дорожная карта до v1.0

Каждая версия имеет проверяемый финиш — его можно дословно отдать в `/goal` (готовые формулировки в `PROMPTS.md`). Общее для всех: `tools/check.py` → PASS, `qa/qa.py` → `QA RESULT: PASS` на mobile и desktop, запись в `CHANGELOG.md`.

| Версия | Тема | Роли | Готово, когда |
|---|---|---|---|
| **0.95.0** ✅ | Порядок штаба, иконки, версия, читаемость меню | ux-ui, tech-artist, engineer | сделано: `launch_first_screen: true`, в меню ≤ 3 элементов мельче 11 px |
| **0.96.0** | HUD боя | ux-ui, engineer, qa-reviewer | в бою нет текста мельче 10 px; ресурсы, линии и поддержка читаются на 390 px; кнопки поддержки ≥ 44 px; подсказка первого боя ведёт по шагам |
| **0.97.0** | Награды R1–R4 | game-designer → engineer | расчёт «новый игрок, 3 боя в день» показывает долю боя ≥ 60 % дохода первой недели; старые сохранения грузятся без ошибок |
| **0.98.0** | Визуал карт и моделей | tech-artist, qa-reviewer | у всех 82 карт своё превью (нет повторов картинок в коллекции, кроме значков); новые ассеты описаны в `docs/ASSETS.md`; рост сборки ≤ 3 МБ |
| **0.99.0** | Стили и техдолг | engineer | `00-base.css` сведён (дубли селекторов удалены), скриншоты всех экранов совпадают с 0.98 визуально; миграции кейсов — одна таблица |
| **1.0.0** | Релиз | producer, qa-reviewer | прогон на реальном телефоне: 30+ FPS в бою 220 очков на «Сбалансированном», полный проход M01–M05 без ошибок консоли |

Решения, которые принимает Максим: сведение валют (R5 в `REWARDS_REVIEW.md`), удаление или перенос экранов «Ещё», минимальное железо для 1.0.

# Журнал изменений

## 0.99.1 — терминал: пустой исход стал малым пакетом (R2)

Для игрока:
- Исход «Без награды», который выпадал в 40 % прокрутов и не давал ничего, теперь приносит одну деталь модуля и называется «Малый пакет».
- Уже сыгранные прокруты в журнале показываются так, как были оплачены: старые записи остаются без награды, а не задним числом «дорисовываются».

Изменения баланса (было → стало):

| Что | Было | Стало |
|---|---|---|
| Исход `empty`, 40 % прокрутов | 0 | 1 деталь модуля |
| Исход `parts` | 3 детали | 3 детали (без изменений) |
| Исход `shards` | 3 осколка | 3 осколка (без изменений) |
| Веса исходов, пулы карт, гарантия `pityAt=8`, поток ГПСЧ | — | без изменений |

Для кода (симуляция не менялась; формат сохранений расширен с миграцией):
- `game/progress.js`: появилась вторая версия политики терминала. `TERMINAL_PAYOUT` — таблица выплат по версиям, `terminalPayout(sequence, v2From, outcome)` выбирает нужную.
- `terminal.v2From` — новое поле журнала: последний номер прокрута, оплаченный по старой таблице. Сохранение версии 1 переводится на версию 2 при загрузке, `v2From` получает текущий `seq`, поэтому вся прежняя история остаётся валидной.
- Номер политики сознательно не пишется в саму квитанцию: набор её полей проверяется на точное совпадение, и лишнее поле сломало бы ранее записанные квитанции.
- `ui/views.js` и `data/design-config.js`: подписи исхода. Экран результата берёт значение из самой квитанции, поэтому старый прокрут показывается как «Без награды», а новый — как «Малый пакет».
- Все правки — `patches/v0_99_1/spec.py` (12 шагов, идемпотентно).

Как проверено: в 0.99.0 сделано 14 прокрутов, сохранение перенесено в 0.99.1 — импорт без ошибок, журнал мигрировал на версию 2 с `v2From = 14`, все 14 записей на месте. Следующие 14 прокрутов оплачены по новой таблице: `empty` даёт 1 деталь, `parts` — 3, `shards` — 3. В итоговом журнале пустые исходы до миграции (№ 5, 6, 7, 9, 13) по-прежнему с нулём, после (№ 15, 20–25, 28) — с единицей. Ошибок консоли нет. То, что старые квитанции прошли пересчёт, доказывает и неизменность самого броска: при любом сдвиге ГПСЧ импорт бы упал.

## 0.99.0 — стили и техдолг

Для игрока: ничего не меняется. Обе правки — доказанные эквивалентности, экраны и наборы карт остаются прежними.

Для кода (без изменений симуляции, баланса и формата сохранений):
- `css/00-base.css`: удалены 27 блоков, которые перекрывались более поздним правилом с тем же селектором в том же `@media`-контексте, — 1 175 байт. Остальные повторы селекторов (всего повторяется 639) живут в разных контекстах и несут нагрузку, поэтому не тронуты.
- `game/progress.js`: тридцать шесть таблиц наборов контейнеров (`CRATE_POOLS28`…`CRATE_POOLS90`), выводившихся одна из другой, сведены в одну таблицу с теми же 37 политиками. `cratePolicy` сохранения указывает на тот же ключ, поэтому старый профиль тянет карты из того же набора. История — что добавляла каждая версия — сохранена справкой над таблицей.
- `tools/cssdedupe.py` — инструмент, который находит и удаляет доказуемо мёртвые блоки стилей.
- Все правки — `patches/v0_99_0/spec.py` (32 шага, идемпотентно: повторный прогон даёт 32 skip).

Как проверено:
- Скриншоты пяти разделов меню и настроек на 390 × 844 и 1440 × 900 сверены с 0.98.1 попиксельно: худшее расхождение 0,012 % при пороге 0,05 %.
- Сведённая таблица наборов сверена с прежней цепочкой после JSON-обхода: структуры идентичны.
- Перенос сохранения 0.98.1 → 0.99.0: импорт без ошибок.

Важно по ходу работы: первая версия `cssdedupe.py` не распознавала `@media`, если перед ним стоял перевод строки, и складывала правила из разных контекстов в одну группу. Она насчитала 278 «мёртвых» блоков; удаление сломало десктоп — сверка скриншотов показала расхождение до 24 %. После починки разбора честное число оказалось 27 блоков вместо 278.

## 0.98.1 — портреты коллекции

Для игрока:
- Карты «Гренадёр» и «Ракетный» больше не показывают картинку здания, которое их выпускает: у них свои портреты — гренадёр и ракетная установка.

Для кода (без изменений симуляции, баланса и формата сохранений):
- `data/card-previews981.js` — новая часть сборки: два портрета 192 px WebP, снятые с тех же моделей, что игра рисует в бою, и слитые поверх `TERRA_ASSETS`.
- `ui/views.js`: `cardImage` сначала смотрит `card981-<id>.webp`, и только потом идёт по прежней цепочке запасных вариантов.
- `docs/ASSETS.md` — где лежат ассеты, как сняты портреты и почему две пары карт остаются с общим изображением.
- Все правки — `patches/v0_98_1/spec.py` (6 шагов, идемпотентно).

Замер коллекции до и после: 82 карты, уникальных изображений 78 → 80. Остались две пары — `stormInterceptor`/`airBarrage` и `pulseKite`/`pulseSting`: в данных игры это одна модель на два снаряжения, подробности в `docs/ASSETS.md`.
Рост сборки: 4 256 байт при бюджете 3 МБ.

## 0.98.0 — награды: повторы и полевая добыча (задача баланса)

Для игрока:
- Повторная победа в операции приносит 35 % от награды за первую победу — для первых пяти повторов в день. Дальше действует прежнее табличное значение, чтобы одна операция не превращалась в ферму.
- Находки в бою стали заметными: сплав с находки 6–12 вместо 3–7.

Изменения баланса (было → стало):

| Что | Было | Стало |
|---|---|---|
| Повтор M01 (сплав) | 40 | 63 (35 % от 180) |
| Повтор M02 (сплав) | 60 | 91 (35 % от 260) |
| Повтор M03 (сплав) | 80 | 140 (35 % от 400) |
| Повторов в день по формуле | без лимита, плоская ставка | 5, дальше прежняя таблица |
| Сплав с одной находки | 3–7 | 6–12 |
| Доля боя в доходе первой недели | 92,4 % | 94,1 % |

Для кода (симуляция и формат сохранений не менялись):
- `game/progress.js`: `REPEAT_SHARE_098`, `REPEAT_CAP_098` и `repeatReward098(m,mission)`. Счётчик повторов живёт в `missionsDaily.repeats` и сбрасывается вместе с остальными дневными счётчиками; `normalize()` сливает его поверх дефолтов, поэтому сохранение, записанное до этой версии, грузится со счётчиком 0.
- `data/design-config.js`: `TERRA_LOOT_CONFIG.types.alloy` — `min` 3 → 6, `max` 7 → 12.
- `tools/economy.py` — новый счётчик экономики: читает поставляемые числа и печатает, откуда новый игрок берёт сплав за первую неделю, с вердиктом `BATTLE SHARE: PASS|FAIL`.
- Все правки — `patches/v0_98_0/spec.py` (9 шагов, идемпотентно: повторный прогон даёт 9 skip).

Что НЕ сделано из разбора наград и почему — см. `REWARDS_REVIEW.md`: R2 ломает формат сохранений, R3 опирается на неверные числа.

QA: `check.py` PASS. `tools/economy.py`: доля боя 94,1 % при цели ≥ 60 %. Перенос сохранения 0.97.1 → 0.98.0 в браузере: импорт без ошибок, кошелёк и 82 карты на месте, новый счётчик повторов = 0.

## 0.97.1 — текстуры моделей под устройство, крен на поворотах

Для игрока:
- Игра занимает меньше видеопамяти на телефоне: текстуры моделей теперь масштабируются под качество, а не грузятся в исходном размере на любом устройстве. Замер на телефоне: 23,4 МБ → 18,8 МБ, все 21 модель и 22 текстуры на месте.
- Техника кренится в поворот, а не проходит его как по рельсам.

Для кода (без изменений симуляции, баланса и формата сохранений):
- `render/adapters.js`: встроенные текстуры GLB перед загрузкой уменьшаются до потолка качества (512 / 1024 / 2048), счётчик `textureBytes` считает то, что реально загружено. Потолок ставится в `prewarm`, до первой загрузки модели.
- `render/adapters.js`: у `rootInto` появился канал крена — поворот вокруг собственной оси движения, так что тангаж и курс при этом сохраняются; `TerraPose.Actor.update` принимает угол крена десятым аргументом.
- `render/adapters.js`: крен считается от скорости изменения курса, измеренной по собственному предыдущему курсу render-актора (`heading0971`), с ограничением и экспоненциальным сглаживанием. Из симуляции ничего не читается и в неё ничего не пишется.
- Все правки — `patches/v0_97_1/spec.py` (11 шагов, идемпотентно: повторный прогон даёт 11 skip).

QA: `check.py` PASS (7 блоков скриптов). `qa.py` меню mobile и desktop — PASS, `launch_first_screen: true`; бой mobile — PASS, `dock_stable: true`, `tinyText` пуст, ошибок консоли нет.

## 0.97.0 — модели юнитов: детализация, текстуры, движение

Для игрока:
- Поверхности техники, зданий и земли стали чётче на косых углах: анизотропная фильтрация поднята с 4× до 8× там, где видеокарта это позволяет (на слабом качестве по-прежнему 1×).
- Юниты перестали быть жёстко вертикальными: корпус приседает при разгоне, клюёт носом при остановке и наклоняется на уклонах.
- Подробные модели теперь выбираются по реальному размеру юнита на экране, а не по «зуму больше 1.22»: на телефоне они появляются при приближении, на большом экране — сразу.

Для кода (без изменений симуляции, баланса и формата сохранений):
- `render/adapters.js`: `Battlefield.actorLean097` — наклон корпуса от ускорения и уклона местности. Состояние (`speed097`, `lean097`) живёт на render-акторе, в `sim.s` не пишется, `Math.random` и `Date.now` не используются; сглаживание экспоненциальное по render-dt.
- `render/adapters.js`: `Battlefield.frame097` — один расчёт `detailScale097` (пикселей на метр реального буфера кадра, с учётом `renderScale`) и бюджет подробных моделей на кадр: 24 на высоком качестве, 12 на среднем. Порог 6.5 px/м с гистерезисом 5.8.
- `render/adapters.js` и `engine/ark-visual-a.js`: потолок анизотропии 4 → 8 для текстур импортированных моделей и массива материалов.
- `metrics()` отдаёт `detailScale097` и `detailActors097` для проверки.
- Все правки — `patches/v0_97_0/spec.py` (11 шагов, идемпотентно: повторный прогон даёт 11 skip).

QA: `check.py` PASS (7 блоков скриптов). `qa.py` меню mobile и desktop — PASS, `launch_first_screen: true`; бой mobile — PASS, `dock_stable: true`, `tinyText` пуст.
Замер в бою: анизотропия 4 → 8; масштаб экрана 2.4–3.5 px/м на телефоне 390 px и 9.2 на 1440 px, поэтому подробные модели включаются на большом экране и при приближении на телефоне, как и раньше, но теперь по реальному размеру, а не по числу зума. Число частей и треугольников при 10 юнитах не изменилось (310 частей, 51 398 треугольников), ошибок консоли нет.

## 0.96.0 — HUD боя

Для игрока:
- В бою не осталось подписей мельче 10 px: «КОРПУС» и «ПРОТИВНИК», фаза операции, чипы целей и маяка, «/100» и «/220»
  в ресурсах, счёт постов, стоимость поддержки, метки площадок на карте и подписи карточек строительства в альбомной ориентации.
- Первый бой ведёт по шагам: «стройка → улучшение → поддержка». Подсказка больше не исчезает после первой постройки и сменяется на следующий шаг по вашим действиям.
- Подсказки обучения и советы больше не обрезаются многоточием: длинная строка переносится, а не теряет конец.
- Крестик «Закрыть строительство» и отмена поддержки стали полноразмерной целью касания 44 px (были 40 × 26 и 44 × 32).
- Чип «Цели операции» расширен до 190 px, чтобы подпись 11 px помещалась целиком.

Для кода (без изменений симуляции, баланса и формата сохранений):
- `css/11-hud96.css` — новый последний слой стилей; правила привязаны к телефонным брейкпойнтам, размеры десктопа (11–12 px) не уменьшаются.
- `ui/controller.js`: `FIRST_STEPS_096` и `firstBattleHint096(s)` — три шага читаются только из живого состояния симуляции (`structures`, `tier`, `teams[0].cooldowns`) и из `meta.matches===0`. Ничего не сохраняется, не начисляется и не попадает в кошелёк; для миссий шаги по-прежнему ведёт `TerraGuide`.
- Все правки — `patches/v0_96_0/spec.py` (6 шагов + новый слой CSS, идемпотентно).

QA: `check.py` PASS (7 блоков скриптов). `qa.py` меню mobile и desktop — PASS, `launch_first_screen: true`; бой mobile — PASS, `dock_stable: true`, постройка ставится.
Замер HUD: минимальный кегль в бою 10 px на 390 × 844, 844 × 390 и 1440 × 900 (было 8–9). Элементов мельче 10 px на телефоне — 0 (было 12), целей касания меньше 44 px — 0 (была 1, крестик 40 × 26).

## 0.95.0 — порядок, иконки, читаемость

Для игрока:
- Штаб: сначала рекомендованный шаг, затем запуск боя, затем ветки прогресса и служебные блоки. На телефоне «В БОЙ» видна без прокрутки.
- «Боевые отчёты» и «Пять действий командира» больше не сжимаются в узкую колонку внутри карточки AGI.
- Нижняя навигация: пять нарисованных иконок одного стиля, у активной вкладки заливка.
- Иконка во вкладке браузера и на домашнем экране телефона, эмблема на экране загрузки.
- Одна версия везде: 0.95.0.
- Фильтры карт на телефоне прокручиваются одной строкой; календарь наград компактнее: на телефоне на один экран прокрутки меньше.
- Второстепенный текст в меню не мельче 11 px, кнопки сложности и призывы AGI — 44 px.
- Понятнее подсказка в бою и тексты настроек; «Emperor» → «Император».

Для кода (без изменений симуляции, баланса и формата сохранений):
- `ui/version95.js` — `globalThis.TERRA_VERSION`; `ui/icons95.js` — `TerraIcons95`; `css/10-polish95.css` — последний слой стилей.
- `ui/controller.js`: HQ-095 (явный порядок блоков), иконки навигации из `TerraIcons95`, версия из `TERRA_VERSION` в отчёте устройства, `TerraHealth` и настройках.
- Все правки — `patches/v0_95_0/spec.py` (16 шагов, идемпотентно).

QA: `check.py` PASS (7 блоков скриптов). `qa.py` меню mobile и desktop — PASS, `launch_first_screen: true`; бой mobile — PASS, постройка ставится, `dock_stable: true`.
